unprocessed_dir: ~
done_dir: ~
error_dir: ~
send_heartbeat: true
heartbeat_seconds: 21600
s3:
  bucket: prod-salk-hpi
  bucket_dir: image/plate/raw/ # aka key prefix. must include '/' at the end
cloudwatch:
  log_group: s3-desktop-uploader # must already exist
  stream_name: ~
  send_interval: 1 # in seconds
upload_device_id: ~
