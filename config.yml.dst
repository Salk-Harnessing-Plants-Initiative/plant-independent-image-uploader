unprocessed_dir: ~
done_dir: ~
error_dir: ~
send_heartbeat: true
heartbeat_seconds: 60
s3:
  bucket: ~
  bucket_dir: ~ # aka key prefix. must include '/' at the end
cloudwatch:
  log_group: ~ # must already exist
  stream_name: ~
  send_interval: 30 # in seconds
upload_device_id: ~
